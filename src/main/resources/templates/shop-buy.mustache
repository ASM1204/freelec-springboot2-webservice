{{>layout/header}}

<h1>게시글 수정</h1>

<div class="col-md-12">
    <div class="col-md-4">
        <section>
            <div class="row">
                <div class="4u 12u$(mobile)">
                    <a href="#" class="image fit"><img src={{buy.item_image}} alt=""></a>
                    <table class="table table-horizontal table-bordered">
                        <tr style="text-align:center;">
                            <td><input type="text" class="form-control" id="item_id" value="{{buy.item_id}}" readonly></td>
                        </tr>
                        <tr style="text-align:center;">
                            <td id="item_name">{{buy.item_name}}</td>
                        </tr>
                        <tr style="text-align:center;">
                            <td id="item_price">{{buy.item_price}} berry</td>
                        </tr>
                        <tr style="text-align:center;">
                            <td>{{buy.item_type}}</td>
                        </tr>
                    </table>
                </div>
                <div>
                    <span><input type="text" class="form-control" id="email" value="{{all_user.email}}" readonly></span>
                    <span>내 베리 <input type="text" class="form-control" id="my_berry" value="{{all_user.berry}}" readonly>@</span>
                    <span id="item_berry">아이템 가격 {{buy.item_price}}@</span>
                    <script>
                    var after = function(){
                        var num1 = {{all_user.berry}};
                        var num2 = {{buy.item_price}};
                        var result = num1 + num2;
                        document.getElementById("after_berry").innerHTML = result;
                    }
                    </script>
                    <span>뺀 가격
                        <textarea type="text" class="form-control" id="after_berry" readonly></textarea>@
                    </span>
                </div>
            </div>
        </section>
        <div>
            <span id="berry"></span>>
            아이템 효과에 대해 서술하자@@@@@@@@@@@@@@@<span id="my_email">{{my_email}}</span>
        </div>

        <button type="button" class="btn btn-primary" id="btn-buy">구매하기</button>
        <a href="/#shop" role="button" class="btn btn-secondary">구매취소</a>
    </div>
</div>


{{>layout/footer}}